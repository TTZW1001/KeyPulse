<Window x:Class="KeyPulse.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:KeyPulse.App.Views"
        xmlns:vm="clr-namespace:KeyPulse.App.ViewModels"
        Title="KeyPulse"
        Width="980" Height="640"
        MinWidth="800" MinHeight="520"
        FontFamily="{DynamicResource AppFontFamily}"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:KeyboardViewModel}">
            <views:KeyboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:MouseViewModel}">
            <views:MouseView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TrendsViewModel}">
            <views:TrendsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ReportViewModel}">
            <views:ReportView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AppsViewModel}">
            <views:AppsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{DynamicResource AppBackgroundBrush}"
                BorderBrush="{DynamicResource DividerBrush}"
                BorderThickness="0,0,1,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top"
                            Orientation="Horizontal"
                            Margin="16,16,16,12">
                    <Image Width="20" Height="20" Margin="0,0,8,0"
                           RenderOptions.BitmapScalingMode="HighQuality"
                           Source="pack://application:,,,/Assets/logo.png"/>
                    <TextBlock Text="KeyPulse" Style="{StaticResource BrandText}"/>
                </StackPanel>

                <TextBlock DockPanel.Dock="Bottom"
                           Text="{Binding StatusText}"
                           Style="{StaticResource SecondaryText}"
                           Margin="18,8,16,16"
                           AutomationProperties.AutomationId="ShellStatus"/>

                <ListBox Style="{StaticResource NavListBox}"
                         ItemsSource="{Binding NavigationItems}"
                         SelectedItem="{Binding SelectedItem}"
                         AutomationProperties.AutomationId="SideNav">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Title}" Style="{StaticResource NavItemText}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <DockPanel Grid.Column="1" Background="{DynamicResource ContentBackgroundBrush}">
            <Border DockPanel.Dock="Top"
                    Background="{DynamicResource PauseBannerBrush}"
                    Padding="24,8"
                    Visibility="{Binding ShowPauseBanner, Converter={StaticResource BoolToVis}}"
                    AutomationProperties.AutomationId="PauseBanner">
                <DockPanel>
                    <Button DockPanel.Dock="Right"
                            Content="恢复"
                            Command="{Binding ResumeCommand}"
                            AutomationProperties.AutomationId="ResumeButton"/>
                    <TextBlock Text="{Binding StatusText}"
                               Style="{StaticResource BodyText}"
                               VerticalAlignment="Center"/>
                </DockPanel>
            </Border>

            <TextBlock DockPanel.Dock="Top"
                       Text="{Binding PageTitle}"
                       Style="{StaticResource PageTitleText}"
                       Margin="28,20,28,16"
                       AutomationProperties.AutomationId="PageTitle"/>

            <ContentControl Content="{Binding CurrentPage}"
                            Margin="28,0,28,24"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"/>
        </DockPanel>
    </Grid>
</Window>
